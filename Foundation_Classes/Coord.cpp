#include <cmath>
#include "Coord.h"

Coord min(Coord x, Coord y)
{
    return min(x, y);
}

Coord max(Coord x, Coord y)
{
    return max(x, y);
}

Coord abs(Coord x)
{
    return abs(x);
}

int round(Coord x)
{
    return floor(x + 0.5);
}
