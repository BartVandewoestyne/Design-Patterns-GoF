#ifndef ROW_H
#define ROW_H

class Row {
    // TODO: Glyph subclasses that have Children (e.g., Row and Column) must
    // implement Next so that it calls GlyphContext::Next at each point in the
    // traversal (See book page 202).
};

#endif /* ROW_H */
